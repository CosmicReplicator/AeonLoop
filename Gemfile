# Gemfile  (root of the repo)
source "https://rubygems.org"

gem "jekyll", "~> 4.3"

group :jekyll_plugins do
  gem "jekyll-mermaid", "~> 1.0"
end

# (optional, Windows file-watch speed-up)
gem "wdm", ">= 0.1.0", platforms: [:mingw, :mswin, :x64_mingw]


